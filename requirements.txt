torch>=2.2.0
numpy>=1.24.0
triton>=2.2.0

# Required by the vendored Moonshot Kimi K3 tokenizer (verace_v1/tokenizer/)
tiktoken>=0.7.0
tokenizers>=0.19.0
transformers>=4.40.0

# Development / testing only — not required to use the package
pytest>=7.0.0
